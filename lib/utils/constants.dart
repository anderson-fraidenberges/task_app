import 'package:flutter/material.dart';

class Consts {
  static const colorPaleWhite = Color.fromRGBO(245, 247, 249, 1);

  static const colorSlateBlue = Color(0xFF8D9CB8);

  static const colorSlatePurple = Color(0xFF3F3D56);

  static const colorBlue = Color(0xFF007FFF);

  static const colorMutedAzure = Color(0xFFC6CFDC);

  static const colorButton = Color.fromRGBO(0, 127, 255, 0.1);

  static const colorFireRed = Color.fromRGBO(255, 94, 94, 1);
}
